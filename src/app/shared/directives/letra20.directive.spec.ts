import { Letra20Directive } from './letra20.directive';

describe('Letra20Directive', () => {
  it('should create an instance', () => {
    const directive = new Letra20Directive();
    expect(directive).toBeTruthy();
  });
});
